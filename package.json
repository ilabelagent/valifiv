{
  "name": "valifi-platform",
  "version": "1.0.0",
  "description": "AI-Powered Financial Platform",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "dev": "vercel dev",
    "build": "echo 'Build step not required for this static setup'",
    "start": "vercel deploy --prod"
  },
  "devDependencies": {
    "vercel": "^35.2.1"
  }
}